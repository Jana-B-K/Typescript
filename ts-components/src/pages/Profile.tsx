import ProfileSection from '../components/ProfileSection'

function Profile() {
  return (
    <ProfileSection
      name="Jana BK"
      role="Frontend Developer"
      bio="This profile page is built with a typed component using a TypeScript interface."
    />
  )
}

export default Profile
